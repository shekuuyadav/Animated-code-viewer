{
  "entities": {
    "Image": {
      "title": "Image",
      "type": "object",
      "description": "Represents an image that is associated with a QR code.",
      "properties": {
        "fileName": {
          "type": "string",
          "description": "The name of the image file."
        },
        "url": {
          "type": "string",
          "description": "The URL where the image is stored.",
          "format": "uri"
        },
        "uploadedAt": {
          "type": "string",
          "description": "Timestamp indicating when the image was uploaded.",
          "format": "date-time"
        },
        "userId": {
          "type": "string",
          "description": "The UID of the user who uploaded the image."
        }
      },
      "required": [
        "fileName",
        "url",
        "uploadedAt",
        "userId"
      ]
    }
  },
  "auth": {
    "providers": [
      "anonymous"
    ]
  },
  "firestore": {
    "/images/{imageId}": {
      "schema": {
        "$ref": "#/entities/Image"
      },
      "description": "Collection to store images uploaded by users."
    }
  }
}
